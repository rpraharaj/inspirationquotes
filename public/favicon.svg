<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" fill="none">
  <!-- Agent/Bot Icon - matches header icon -->
  <!-- Body -->
  <rect x="5" y="11" width="22" height="16" rx="3" stroke="currentColor" stroke-width="2.5"/>
  
  <!-- Antenna -->
  <path d="M16 11V5H11" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
  
  <!-- Left ear -->
  <path d="M2 18h3" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"/>
  
  <!-- Right ear -->
  <path d="M27 18h3" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"/>
  
  <!-- Left eye -->
  <path d="M12 17v3" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"/>
  
  <!-- Right eye -->
  <path d="M20 17v3" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"/>
  
  <style>
    svg { color: #000; }
    @media (prefers-color-scheme: dark) {
      svg { color: #FFF; }
    }
  </style>
</svg>
